[pytest]
pythonpath = .

# live 표시가 붙은 테스트는 실제 DB와 LM Studio가 있어야 돌아간다.
# 기본 실행에서는 빼고, 필요할 때만 `pytest -m live`로 부른다
# (명령줄 -m이 여기 addopts를 덮어쓴다).
markers =
    live: 실제 DB·LM Studio가 필요한 회귀 테스트
addopts = -m "not live"
